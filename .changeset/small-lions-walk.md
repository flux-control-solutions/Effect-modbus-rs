---
'@flux-control/effect-modbus-rs': patch
---

Update the browser integration for `modbus-rs@0.16.1`, which restores runtime export compatibility for the `modbus-rs/web` WASM module. API documentation generation now uses a TypeScript 6 toolchain compatible with TypeDoc.
